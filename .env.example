# ============================================================
# GemAI — Vercel environment variables (.env.example)
# Copy to .env (for `vercel dev`) and add to Vercel → Settings → Environment Variables.
# ============================================================

# --- Optional: AI chat (the whole app works FREE without this) ---
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=
# Or use any OpenAI-compatible provider:
# OPENAI_API_KEY=
# AI_BASE_URL=https://api.groq.com/openai/v1
# AI_MODEL=llama-3.3-70b-versatile

# --- Optional: Supabase for cloud memory (cross-device) ---
# Create a project at https://supabase.com, run supabase/schema.sql,
# and enable "Anonymous sign-ins" under Authentication → Providers.
SUPABASE_URL=
SUPABASE_ANON_KEY=
